User-agent: *
Allow: /
# Attribution/conversion surface — kept out of organic SERPs so podcast click
# data stays clean. A parallel SEO blog page ranks for these queries instead.
Disallow: /trial/
# /start/ and /coupon/ removed 2026-07-22: both URLs 301 to the money page
# (see _redirects); blocking them hid the redirects from crawlers and stranded
# their link equity. Only /trial/ is still a real (attribution) page.

Sitemap: https://globalhighlevel.com/sitemap.xml

# AI crawlers explicitly allowed (incl. the /trial/ attribution path) — see llms.txt for structured content index
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: anthropic-ai
Allow: /
