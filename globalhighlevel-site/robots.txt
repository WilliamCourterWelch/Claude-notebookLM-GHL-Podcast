User-agent: *
Allow: /
# Attribution/conversion surfaces — kept out of organic SERPs so podcast/blog
# click data stays clean. Parallel SEO blog pages rank for these queries instead.
Disallow: /trial/
# /start/ and /coupon/ removed 2026-07-22: both URLs 301 to the money page
# (see _redirects); blocking them hid the redirects from crawlers and stranded
# their link equity. Only /trial/ is still a real (attribution) page.

Sitemap: https://globalhighlevel.com/sitemap.xml

# AI crawlers explicitly allowed (incl. the attribution paths) — see llms.txt for structured content index
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: anthropic-ai
Allow: /
